import "./globals.css";

export const metadata = {
  title: "Shri Malikarjun Hardware & Paints | Canacona, Goa",
  description: "Shri Malikarjun Hardware & Paints — paints, hardware, tools and project supplies in Canacona, Goa.",
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
