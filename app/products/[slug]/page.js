import Link from "next/link";
import { products } from "../../../lib/catalog";
import { Header, Footer } from "../../components/SiteChrome";
export function generateStaticParams(){return products.map(product=>({slug:product.slug}));}
export default async function ProductPage({params}){const {slug}=await params;const product=products.find(p=>p.slug===slug);if(!product)return <div className="page container"><h1>Product not found</h1></div>;return <><Header/><main className="page"><div className="container"><Link href="/products" className="back">← BACK TO PRODUCTS</Link><div className="product-detail pro-detail"><div className={`detail-art ${product.tone}`}><span>{product.short}</span></div><div><span className="product-category">{product.category}</span><h1>{product.name}</h1><p className="detail-copy">{product.longDescription}</p><div className="spec-list"><div><span>CATEGORY</span><strong>{product.category}</strong></div><div><span>AVAILABILITY</span><strong>CONTACT STORE</strong></div><div><span>SIZES</span><strong>ASK FOR CURRENT OPTIONS</strong></div></div><a className="btn btn-orange" href={`https://wa.me/919999999999?text=${encodeURIComponent(`Hi, I'm interested in ${product.name}. Is it available?`)}`} target="_blank" rel="noreferrer">ENQUIRE ON WHATSAPP</a></div></div></div></main><Footer/></>}
