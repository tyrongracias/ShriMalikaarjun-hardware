import Link from "next/link";
import { products } from "../../lib/catalog";
import { Header, Footer } from "../components/SiteChrome";
export default function ProductsPage(){return <><Header/><main className="page"><div className="container"><p className="kicker">PRODUCT CATALOGUE</p><h1>Products.</h1><p className="lead">Browse our range of paints, hardware, tools, waterproofing and accessories. Contact the store for current availability and sizes.</p><div className="product-grid large pro-grid">{products.map((p,i)=><Link key={p.slug} href={`/products/${p.slug}`} className="product-card pro-card"><div className={`product-art ${p.tone}`}><span className="product-tag">{String(i+1).padStart(2,"0")}</span><span className="product-short">{p.short}</span></div><div className="product-info"><span className="product-category">{p.category}</span><h3>{p.name}</h3><p>{p.description}</p><span className="product-link">VIEW PRODUCT →</span></div></Link>)}</div></div></main><Footer/></>}
